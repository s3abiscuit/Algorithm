## undirected graphs

1. **graph**: representation `adj = Bag<Integer>[]`, `marked = boolean[]`, `pathTo = int[]`, `queue`
2. **search**: from one vertex, search another vertex, Depth first search/ Width first search
3. **path**: from one vertex, path to another vertex
4. connected components

